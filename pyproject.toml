[project]
name = "commit-haiku"
version = "0.1.0"
description = "Generate haikus from your git changes"
requires-python = ">=3.8"

[project.scripts]
commit-haiku = "commit_haiku.__main__:main"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
